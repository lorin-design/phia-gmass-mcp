# GMass API key — get from GMass dashboard → Settings → API Keys
GMASS_API_KEY=your_gmass_api_key_here

# Port the MCP server listens on (Fly.io sets this automatically; default 8000 locally)
PORT=8000
